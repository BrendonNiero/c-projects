#include <stdio.h>

int main(){
    int foo = 1;
    int bar = 2;

    if(foo > bar){
        printf("Foo is more big than bar.");
    } else {
        printf("Bar is more big than foo.");
    }
    return 0;
}